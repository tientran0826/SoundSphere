name: Build & Push Docker

on:
  push:
    branches: ["develop", "production"]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Login Docker
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set Docker tag
        run: |
          DOCKER_REPO="${{ secrets.DOCKERHUB_USERNAME }}"
          if [ "${{ github.ref_name }}" == "production" ]; then
            BOT_TAG="${DOCKER_REPO}/soundsphere-bot:latest"
            APPLICATION_TAG="${DOCKER_REPO}/soundsphere-application:latest"
          else
            BOT_TAG="${DOCKER_REPO}/soundsphere-bot-dev:latest"
            APPLICATION_TAG="${DOCKER_REPO}/soundsphere-application-dev:latest"
          fi
          echo "TAG=$TAG" >> $GITHUB_ENV

      - name: Build & Push Bot Image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ env.BOT_TAG }}

      - name: Build & Push Application Image
        uses: docker/build-push-action@v4
        with:
          context: ./application
          push: true
          tags: ${{ env.APPLICATION_TAG }}

      - name: Trigger Argo CD sync BOT
        run: |
          if [ "${{ github.ref_name }}" == "production" ]; then
            APP_NAME="soundsphere"
          else
            APP_NAME="soundsphere-dev"
          fi

          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.ARGOCD_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"prune":true,"force":true,"replace":true}' \
            https://argocd.castl.io.vn/api/v1/applications/$APP_NAME/sync

      - name: Trigger Argo CD sync APPLICATION
        run: |
          if [ "${{ github.ref_name }}" == "production" ]; then
            APP_NAME="soundsphere-application"
          else
            APP_NAME="soundsphere-application-dev"
          fi

          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.ARGOCD_TOKEN }}" \
            -H "Content-Type: application/json" \
            -d '{"prune":true,"force":true,"replace":true}' \
            https://argocd.castl.io.vn/api/v1/applications/$APP_NAME/sync
