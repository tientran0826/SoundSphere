apiVersion: v1
kind: Service
metadata:
  name: soundsphere-bot
  namespace: production
spec:
  selector:
    app: soundsphere-bot
  type: NodePort
  ports:
    - name: fastapi
      protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30081
