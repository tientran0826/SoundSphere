apiVersion: v1
kind: Service
metadata:
  name: soundsphere-bot
  namespace: production
spec:
  selector:
    app: soundsphere-bot
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
